/*
 * DiscourseApiDocumentationLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package com.example.discourse.apis;

import com.example.discourse.ApiHelper;
import com.example.discourse.Server;
import com.example.discourse.exceptions.ApiException;
import com.example.discourse.http.request.HttpMethod;
import com.example.discourse.http.response.ApiResponse;
import com.example.discourse.models.SearchJsonResponse;
import io.apimatic.core.ApiCall;
import io.apimatic.core.GlobalConfiguration;
import io.apimatic.coreinterfaces.http.request.ResponseClassType;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;

/**
 * This class lists all the endpoints of the groups.
 */
public final class SearchApi extends BaseApi {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public SearchApi(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * @param  q  Optional parameter: The query string needs to be url encoded and is made up of the
     *         following options: - Search term. This is just a string. Usually it would be the
     *         first item in the query. - `{@literal @}&lt;username&gt;`: Use the `{@literal @}`
     *         followed by the username to specify posts by this user. - `#&lt;category&gt;`: Use
     *         the `#` followed by the category slug to search within this category. - `tags:`:
     *         `api,solved` or for posts that have all the specified tags `api+solved`. - `before:`:
     *         `yyyy-mm-dd` - `after:`: `yyyy-mm-dd` - `order:`: `latest`, `likes`, `views`,
     *         `latest_topic` - `assigned:`: username (without `{@literal @}`) - `in:`: `title`,
     *         `likes`, `personal`, `messages`, `seen`, `unseen`, `posted`, `created`, `watching`,
     *         `tracking`, `bookmarks`, `assigned`, `unassigned`, `first`, `pinned`, `wiki` -
     *         `with:`: `images` - `status:`: `open`, `closed`, `public`, `archived`, `noreplies`,
     *         `single_user`, `solved`, `unsolved` - `group:`: group_name or group_id -
     *         `group_messages:`: group_name or group_id - `min_posts:`: 1 - `max_posts:`: 10 -
     *         `min_views:`: 1 - `max_views:`: 10 If you are using cURL you can use the `-G` and the
     *         `--data-urlencode` flags to encode the query: ``` curl -i -sS -X GET -G
     *         "http://localhost:3000/search.json" \ --data-urlencode 'q=wordpress {@literal
     *         @}scossar #fun after:2020-01-01' ```
     * @param  page  Optional parameter:
     * @return    Returns the SearchJsonResponse wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<SearchJsonResponse> search(
            final String q,
            final Integer page) throws ApiException, IOException {
        return prepareSearchRequest(q, page).execute();
    }

    /**
     * @param  q  Optional parameter: The query string needs to be url encoded and is made up of the
     *         following options: - Search term. This is just a string. Usually it would be the
     *         first item in the query. - `{@literal @}&lt;username&gt;`: Use the `{@literal @}`
     *         followed by the username to specify posts by this user. - `#&lt;category&gt;`: Use
     *         the `#` followed by the category slug to search within this category. - `tags:`:
     *         `api,solved` or for posts that have all the specified tags `api+solved`. - `before:`:
     *         `yyyy-mm-dd` - `after:`: `yyyy-mm-dd` - `order:`: `latest`, `likes`, `views`,
     *         `latest_topic` - `assigned:`: username (without `{@literal @}`) - `in:`: `title`,
     *         `likes`, `personal`, `messages`, `seen`, `unseen`, `posted`, `created`, `watching`,
     *         `tracking`, `bookmarks`, `assigned`, `unassigned`, `first`, `pinned`, `wiki` -
     *         `with:`: `images` - `status:`: `open`, `closed`, `public`, `archived`, `noreplies`,
     *         `single_user`, `solved`, `unsolved` - `group:`: group_name or group_id -
     *         `group_messages:`: group_name or group_id - `min_posts:`: 1 - `max_posts:`: 10 -
     *         `min_views:`: 1 - `max_views:`: 10 If you are using cURL you can use the `-G` and the
     *         `--data-urlencode` flags to encode the query: ``` curl -i -sS -X GET -G
     *         "http://localhost:3000/search.json" \ --data-urlencode 'q=wordpress {@literal
     *         @}scossar #fun after:2020-01-01' ```
     * @param  page  Optional parameter:
     * @return    Returns the SearchJsonResponse wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<SearchJsonResponse>> searchAsync(
            final String q,
            final Integer page) {
        try {
            return prepareSearchRequest(q, page).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for search.
     */
    private ApiCall<ApiResponse<SearchJsonResponse>, ApiException> prepareSearchRequest(
            final String q,
            final Integer page) {
        return new ApiCall.Builder<ApiResponse<SearchJsonResponse>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/search.json")
                        .queryParam(param -> param.key("q")
                                .value(q).isRequired(false))
                        .queryParam(param -> param.key("page")
                                .value(page).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, SearchJsonResponse.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }
}