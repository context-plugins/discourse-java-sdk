/*
 * DiscourseApiDocumentationLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package com.example.discourse.apis;

import com.example.discourse.ApiHelper;
import com.example.discourse.DateTimeHelper;
import com.example.discourse.Server;
import com.example.discourse.exceptions.ApiException;
import com.example.discourse.http.request.HttpMethod;
import com.example.discourse.http.response.ApiResponse;
import com.example.discourse.models.DiscoursePostEventEventsJsonResponse;
import com.example.discourse.models.IncludeDetails;
import com.example.discourse.models.IncludeSubcategories;
import com.example.discourse.models.Order;
import io.apimatic.core.ApiCall;
import io.apimatic.core.GlobalConfiguration;
import io.apimatic.coreinterfaces.http.request.ResponseClassType;
import java.io.IOException;
import java.io.InputStream;
import java.time.LocalDateTime;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;

/**
 * This class lists all the endpoints of the groups.
 */
public final class DiscourseCalendarEventsApi extends BaseApi {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public DiscourseCalendarEventsApi(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * @param  includeDetails  Optional parameter: Include detailed event information (creator,
     *         invitees, stats, etc.)
     * @param  categoryId  Optional parameter: Filter events by category ID
     * @param  includeSubcategories  Optional parameter: Include events from subcategories when
     *         filtering by category
     * @param  postId  Optional parameter: Filter to events associated with a specific post ID
     * @param  attendingUser  Optional parameter: Filter to events where the specified user
     *         (username) has RSVP'd as going
     * @param  before  Optional parameter: Return events starting before this date/time (ISO 8601
     *         format)
     * @param  after  Optional parameter: Return events starting after this date/time (ISO 8601
     *         format)
     * @param  order  Optional parameter: Sort order for events by start date (default: asc)
     * @param  limit  Optional parameter: Maximum number of events to return (default: 200)
     * @return    Returns the DiscoursePostEventEventsJsonResponse wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DiscoursePostEventEventsJsonResponse> listEvents(
            final IncludeDetails includeDetails,
            final Integer categoryId,
            final IncludeSubcategories includeSubcategories,
            final Integer postId,
            final String attendingUser,
            final LocalDateTime before,
            final LocalDateTime after,
            final Order order,
            final Integer limit) throws ApiException, IOException {
        return prepareListEventsRequest(includeDetails, categoryId, includeSubcategories, postId,
                attendingUser, before, after, order, limit).execute();
    }

    /**
     * @param  includeDetails  Optional parameter: Include detailed event information (creator,
     *         invitees, stats, etc.)
     * @param  categoryId  Optional parameter: Filter events by category ID
     * @param  includeSubcategories  Optional parameter: Include events from subcategories when
     *         filtering by category
     * @param  postId  Optional parameter: Filter to events associated with a specific post ID
     * @param  attendingUser  Optional parameter: Filter to events where the specified user
     *         (username) has RSVP'd as going
     * @param  before  Optional parameter: Return events starting before this date/time (ISO 8601
     *         format)
     * @param  after  Optional parameter: Return events starting after this date/time (ISO 8601
     *         format)
     * @param  order  Optional parameter: Sort order for events by start date (default: asc)
     * @param  limit  Optional parameter: Maximum number of events to return (default: 200)
     * @return    Returns the DiscoursePostEventEventsJsonResponse wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DiscoursePostEventEventsJsonResponse>> listEventsAsync(
            final IncludeDetails includeDetails,
            final Integer categoryId,
            final IncludeSubcategories includeSubcategories,
            final Integer postId,
            final String attendingUser,
            final LocalDateTime before,
            final LocalDateTime after,
            final Order order,
            final Integer limit) {
        try {
            return prepareListEventsRequest(includeDetails, categoryId, includeSubcategories, postId,
            attendingUser, before, after, order, limit).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for listEvents.
     */
    private ApiCall<ApiResponse<DiscoursePostEventEventsJsonResponse>, ApiException> prepareListEventsRequest(
            final IncludeDetails includeDetails,
            final Integer categoryId,
            final IncludeSubcategories includeSubcategories,
            final Integer postId,
            final String attendingUser,
            final LocalDateTime before,
            final LocalDateTime after,
            final Order order,
            final Integer limit) {
        return new ApiCall.Builder<ApiResponse<DiscoursePostEventEventsJsonResponse>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/discourse-post-event/events.json")
                        .queryParam(param -> param.key("include_details")
                                .value((includeDetails != null) ? includeDetails.value() : null).isRequired(false))
                        .queryParam(param -> param.key("category_id")
                                .value(categoryId).isRequired(false))
                        .queryParam(param -> param.key("include_subcategories")
                                .value((includeSubcategories != null) ? includeSubcategories.value() : null).isRequired(false))
                        .queryParam(param -> param.key("post_id")
                                .value(postId).isRequired(false))
                        .queryParam(param -> param.key("attending_user")
                                .value(attendingUser).isRequired(false))
                        .queryParam(param -> param.key("before")
                                .value(DateTimeHelper.toRfc8601DateTime(before)).isRequired(false))
                        .queryParam(param -> param.key("after")
                                .value(DateTimeHelper.toRfc8601DateTime(after)).isRequired(false))
                        .queryParam(param -> param.key("order")
                                .value((order != null) ? order.value() : null).isRequired(false))
                        .queryParam(param -> param.key("limit")
                                .value(limit).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DiscoursePostEventEventsJsonResponse.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * @param  categoryId  Optional parameter: Filter events by category ID
     * @param  includeSubcategories  Optional parameter: Include events from subcategories when
     *         filtering by category
     * @param  attendingUser  Optional parameter: Filter to events where the specified user
     *         (username) has RSVP'd as going
     * @param  before  Optional parameter: Return events starting before this date/time (ISO 8601
     *         format)
     * @param  after  Optional parameter: Return events starting after this date/time (ISO 8601
     *         format)
     * @param  order  Optional parameter: Sort order for events by start date (default: asc)
     * @param  limit  Optional parameter: Maximum number of events to return (default: 200)
     * @return    Returns the InputStream wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<InputStream> exportEventsIcs(
            final Integer categoryId,
            final IncludeSubcategories includeSubcategories,
            final String attendingUser,
            final LocalDateTime before,
            final LocalDateTime after,
            final Order order,
            final Integer limit) throws ApiException, IOException {
        return prepareExportEventsIcsRequest(categoryId, includeSubcategories, attendingUser,
                before, after, order, limit).execute();
    }

    /**
     * @param  categoryId  Optional parameter: Filter events by category ID
     * @param  includeSubcategories  Optional parameter: Include events from subcategories when
     *         filtering by category
     * @param  attendingUser  Optional parameter: Filter to events where the specified user
     *         (username) has RSVP'd as going
     * @param  before  Optional parameter: Return events starting before this date/time (ISO 8601
     *         format)
     * @param  after  Optional parameter: Return events starting after this date/time (ISO 8601
     *         format)
     * @param  order  Optional parameter: Sort order for events by start date (default: asc)
     * @param  limit  Optional parameter: Maximum number of events to return (default: 200)
     * @return    Returns the InputStream wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<InputStream>> exportEventsIcsAsync(
            final Integer categoryId,
            final IncludeSubcategories includeSubcategories,
            final String attendingUser,
            final LocalDateTime before,
            final LocalDateTime after,
            final Order order,
            final Integer limit) {
        try {
            return prepareExportEventsIcsRequest(categoryId, includeSubcategories, attendingUser, before,
            after, order, limit).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for exportEventsIcs.
     */
    private ApiCall<ApiResponse<InputStream>, ApiException> prepareExportEventsIcsRequest(
            final Integer categoryId,
            final IncludeSubcategories includeSubcategories,
            final String attendingUser,
            final LocalDateTime before,
            final LocalDateTime after,
            final Order order,
            final Integer limit) {
        return new ApiCall.Builder<ApiResponse<InputStream>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/discourse-post-event/events.ics")
                        .queryParam(param -> param.key("category_id")
                                .value(categoryId).isRequired(false))
                        .queryParam(param -> param.key("include_subcategories")
                                .value((includeSubcategories != null) ? includeSubcategories.value() : null).isRequired(false))
                        .queryParam(param -> param.key("attending_user")
                                .value(attendingUser).isRequired(false))
                        .queryParam(param -> param.key("before")
                                .value(DateTimeHelper.toRfc8601DateTime(before)).isRequired(false))
                        .queryParam(param -> param.key("after")
                                .value(DateTimeHelper.toRfc8601DateTime(after)).isRequired(false))
                        .queryParam(param -> param.key("order")
                                .value((order != null) ? order.value() : null).isRequired(false))
                        .queryParam(param -> param.key("limit")
                                .value(limit).isRequired(false))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .endpointConfiguration(param -> param
                                .hasBinaryResponse(true))
                .build();
    }
}