/*
 * DiscourseApiDocumentationLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package com.example.discourse.apis;

import com.example.discourse.ApiHelper;
import com.example.discourse.Server;
import com.example.discourse.exceptions.ApiException;
import com.example.discourse.http.request.HttpMethod;
import com.example.discourse.http.response.ApiResponse;
import com.example.discourse.models.UploadType;
import com.example.discourse.models.UploadsAbortMultipartJsonRequest;
import com.example.discourse.models.UploadsAbortMultipartJsonResponse;
import com.example.discourse.models.UploadsBatchPresignMultipartPartsJsonRequest;
import com.example.discourse.models.UploadsBatchPresignMultipartPartsJsonResponse;
import com.example.discourse.models.UploadsCompleteExternalUploadJsonRequest;
import com.example.discourse.models.UploadsCompleteExternalUploadJsonResponse;
import com.example.discourse.models.UploadsCompleteMultipartJsonRequest;
import com.example.discourse.models.UploadsCompleteMultipartJsonResponse;
import com.example.discourse.models.UploadsCreateMultipartJsonRequest;
import com.example.discourse.models.UploadsCreateMultipartJsonResponse;
import com.example.discourse.models.UploadsGeneratePresignedPutJsonRequest;
import com.example.discourse.models.UploadsGeneratePresignedPutJsonResponse;
import com.example.discourse.models.UploadsJsonResponse;
import com.example.discourse.utilities.FileWrapper;
import io.apimatic.core.ApiCall;
import io.apimatic.core.GlobalConfiguration;
import io.apimatic.coreinterfaces.http.request.MutliPartRequestType;
import io.apimatic.coreinterfaces.http.request.ResponseClassType;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;

/**
 * This class lists all the endpoints of the groups.
 */
public final class UploadsApi extends BaseApi {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public UploadsApi(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * @param  uploadType  Required parameter:
     * @param  userId  Optional parameter: required if uploading an avatar
     * @param  synchronous  Optional parameter: Use this flag to return an id and url
     * @param  file  Optional parameter:
     * @return    Returns the UploadsJsonResponse wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<UploadsJsonResponse> createUpload(
            final UploadType uploadType,
            final Integer userId,
            final Boolean synchronous,
            final FileWrapper file) throws ApiException, IOException {
        return prepareCreateUploadRequest(uploadType, userId, synchronous, file).execute();
    }

    /**
     * @param  uploadType  Required parameter:
     * @param  userId  Optional parameter: required if uploading an avatar
     * @param  synchronous  Optional parameter: Use this flag to return an id and url
     * @param  file  Optional parameter:
     * @return    Returns the UploadsJsonResponse wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<UploadsJsonResponse>> createUploadAsync(
            final UploadType uploadType,
            final Integer userId,
            final Boolean synchronous,
            final FileWrapper file) {
        try {
            return prepareCreateUploadRequest(uploadType, userId, synchronous, file).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for createUpload.
     */
    private ApiCall<ApiResponse<UploadsJsonResponse>, ApiException> prepareCreateUploadRequest(
            final UploadType uploadType,
            final Integer userId,
            final Boolean synchronous,
            final FileWrapper file) {
        return new ApiCall.Builder<ApiResponse<UploadsJsonResponse>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/uploads.json")
                        .formParam(param -> param.key("upload_type")
                                .value((uploadType != null) ? uploadType.value() : null))
                        .formParam(param -> param.key("user_id")
                                .value(userId).isRequired(false))
                        .formParam(param -> param.key("synchronous")
                                .value(synchronous).isRequired(false))
                        .formParam(param -> param.key("file")
                                .value(file)
                                .multiPartRequestType(MutliPartRequestType.MULTI_PART_FILE)
                                .multipartHeaders("content-type", "application/octect-stream"))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, UploadsJsonResponse.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Direct external uploads bypass the usual method of creating uploads via the POST /uploads
     * route, and upload directly to an external provider, which by default is S3. This route begins
     * the process, and will return a unique identifier for the external upload as well as a
     * presigned URL which is where the file binary blob should be uploaded to. Once the upload is
     * complete to the external service, you must call the POST /complete-external-upload route
     * using the unique identifier returned by this route, which will create any required Upload
     * record in the Discourse database and also move file from its temporary location to the final
     * destination in the external storage service. You must have the correct permissions and CORS
     * settings configured in your external provider. We support AWS S3 as the default. See:
     * https://meta.discourse.org/t/-/210469#s3-multipart-direct-uploads-4. An external file store
     * must be set up and `enable_direct_s3_uploads` must be set to true for this endpoint to
     * function.
     * @param  body  Optional parameter:
     * @return    Returns the UploadsGeneratePresignedPutJsonResponse wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<UploadsGeneratePresignedPutJsonResponse> generatePresignedPut(
            final UploadsGeneratePresignedPutJsonRequest body) throws ApiException, IOException {
        return prepareGeneratePresignedPutRequest(body).execute();
    }

    /**
     * Direct external uploads bypass the usual method of creating uploads via the POST /uploads
     * route, and upload directly to an external provider, which by default is S3. This route begins
     * the process, and will return a unique identifier for the external upload as well as a
     * presigned URL which is where the file binary blob should be uploaded to. Once the upload is
     * complete to the external service, you must call the POST /complete-external-upload route
     * using the unique identifier returned by this route, which will create any required Upload
     * record in the Discourse database and also move file from its temporary location to the final
     * destination in the external storage service. You must have the correct permissions and CORS
     * settings configured in your external provider. We support AWS S3 as the default. See:
     * https://meta.discourse.org/t/-/210469#s3-multipart-direct-uploads-4. An external file store
     * must be set up and `enable_direct_s3_uploads` must be set to true for this endpoint to
     * function.
     * @param  body  Optional parameter:
     * @return    Returns the UploadsGeneratePresignedPutJsonResponse wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<UploadsGeneratePresignedPutJsonResponse>> generatePresignedPutAsync(
            final UploadsGeneratePresignedPutJsonRequest body) {
        try {
            return prepareGeneratePresignedPutRequest(body).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for generatePresignedPut.
     */
    private ApiCall<ApiResponse<UploadsGeneratePresignedPutJsonResponse>, ApiException> prepareGeneratePresignedPutRequest(
            final UploadsGeneratePresignedPutJsonRequest body) {
        return new ApiCall.Builder<ApiResponse<UploadsGeneratePresignedPutJsonResponse>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/uploads/generate-presigned-put.json")
                        .bodyParam(param -> param.value(body).isRequired(false))
                        .bodySerializer(() ->  ApiHelper.serialize(body))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/json").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, UploadsGeneratePresignedPutJsonResponse.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Completes an external upload initialized with /get-presigned-put. The file will be moved from
     * its temporary location in external storage to a final destination in the S3 bucket. An Upload
     * record will also be created in the database in most cases. If a sha1-checksum was provided in
     * the initial request it will also be compared with the uploaded file in storage to make sure
     * the same file was uploaded. The file size will be compared for the same reason. You must have
     * the correct permissions and CORS settings configured in your external provider. We support
     * AWS S3 as the default. See:
     * https://meta.discourse.org/t/-/210469#s3-multipart-direct-uploads-4. An external file store
     * must be set up and `enable_direct_s3_uploads` must be set to true for this endpoint to
     * function.
     * @param  body  Optional parameter:
     * @return    Returns the UploadsCompleteExternalUploadJsonResponse wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<UploadsCompleteExternalUploadJsonResponse> completeExternalUpload(
            final UploadsCompleteExternalUploadJsonRequest body) throws ApiException, IOException {
        return prepareCompleteExternalUploadRequest(body).execute();
    }

    /**
     * Completes an external upload initialized with /get-presigned-put. The file will be moved from
     * its temporary location in external storage to a final destination in the S3 bucket. An Upload
     * record will also be created in the database in most cases. If a sha1-checksum was provided in
     * the initial request it will also be compared with the uploaded file in storage to make sure
     * the same file was uploaded. The file size will be compared for the same reason. You must have
     * the correct permissions and CORS settings configured in your external provider. We support
     * AWS S3 as the default. See:
     * https://meta.discourse.org/t/-/210469#s3-multipart-direct-uploads-4. An external file store
     * must be set up and `enable_direct_s3_uploads` must be set to true for this endpoint to
     * function.
     * @param  body  Optional parameter:
     * @return    Returns the UploadsCompleteExternalUploadJsonResponse wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<UploadsCompleteExternalUploadJsonResponse>> completeExternalUploadAsync(
            final UploadsCompleteExternalUploadJsonRequest body) {
        try {
            return prepareCompleteExternalUploadRequest(body).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for completeExternalUpload.
     */
    private ApiCall<ApiResponse<UploadsCompleteExternalUploadJsonResponse>, ApiException> prepareCompleteExternalUploadRequest(
            final UploadsCompleteExternalUploadJsonRequest body) {
        return new ApiCall.Builder<ApiResponse<UploadsCompleteExternalUploadJsonResponse>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/uploads/complete-external-upload.json")
                        .bodyParam(param -> param.value(body).isRequired(false))
                        .bodySerializer(() ->  ApiHelper.serialize(body))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/json").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, UploadsCompleteExternalUploadJsonResponse.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Creates a multipart upload in the external storage provider, storing a temporary reference to
     * the external upload similar to /get-presigned-put. You must have the correct permissions and
     * CORS settings configured in your external provider. We support AWS S3 as the default. See:
     * https://meta.discourse.org/t/-/210469#s3-multipart-direct-uploads-4. An external file store
     * must be set up and `enable_direct_s3_uploads` must be set to true for this endpoint to
     * function.
     * @param  body  Optional parameter:
     * @return    Returns the UploadsCreateMultipartJsonResponse wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<UploadsCreateMultipartJsonResponse> createMultipartUpload(
            final UploadsCreateMultipartJsonRequest body) throws ApiException, IOException {
        return prepareCreateMultipartUploadRequest(body).execute();
    }

    /**
     * Creates a multipart upload in the external storage provider, storing a temporary reference to
     * the external upload similar to /get-presigned-put. You must have the correct permissions and
     * CORS settings configured in your external provider. We support AWS S3 as the default. See:
     * https://meta.discourse.org/t/-/210469#s3-multipart-direct-uploads-4. An external file store
     * must be set up and `enable_direct_s3_uploads` must be set to true for this endpoint to
     * function.
     * @param  body  Optional parameter:
     * @return    Returns the UploadsCreateMultipartJsonResponse wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<UploadsCreateMultipartJsonResponse>> createMultipartUploadAsync(
            final UploadsCreateMultipartJsonRequest body) {
        try {
            return prepareCreateMultipartUploadRequest(body).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for createMultipartUpload.
     */
    private ApiCall<ApiResponse<UploadsCreateMultipartJsonResponse>, ApiException> prepareCreateMultipartUploadRequest(
            final UploadsCreateMultipartJsonRequest body) {
        return new ApiCall.Builder<ApiResponse<UploadsCreateMultipartJsonResponse>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/uploads/create-multipart.json")
                        .bodyParam(param -> param.value(body).isRequired(false))
                        .bodySerializer(() ->  ApiHelper.serialize(body))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/json").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, UploadsCreateMultipartJsonResponse.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Multipart uploads are uploaded in chunks or parts to individual presigned URLs, similar to
     * the one generated by /generate-presigned-put. The part numbers provided must be between 1 and
     * 10000. The total number of parts will depend on the chunk size in bytes that you intend to
     * use to upload each chunk. For example a 12MB file may have 2 5MB chunks and a final 2MB
     * chunk, for part numbers 1, 2, and 3. This endpoint will return a presigned URL for each part
     * number provided, which you can then use to send PUT requests for the binary chunk
     * corresponding to that part. When the part is uploaded, the provider should return an ETag for
     * the part, and this should be stored along with the part number, because this is needed to
     * complete the multipart upload. You must have the correct permissions and CORS settings
     * configured in your external provider. We support AWS S3 as the default. See:
     * https://meta.discourse.org/t/-/210469#s3-multipart-direct-uploads-4. An external file store
     * must be set up and `enable_direct_s3_uploads` must be set to true for this endpoint to
     * function.
     * @param  body  Optional parameter:
     * @return    Returns the UploadsBatchPresignMultipartPartsJsonResponse wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<UploadsBatchPresignMultipartPartsJsonResponse> batchPresignMultipartParts(
            final UploadsBatchPresignMultipartPartsJsonRequest body) throws ApiException, IOException {
        return prepareBatchPresignMultipartPartsRequest(body).execute();
    }

    /**
     * Multipart uploads are uploaded in chunks or parts to individual presigned URLs, similar to
     * the one generated by /generate-presigned-put. The part numbers provided must be between 1 and
     * 10000. The total number of parts will depend on the chunk size in bytes that you intend to
     * use to upload each chunk. For example a 12MB file may have 2 5MB chunks and a final 2MB
     * chunk, for part numbers 1, 2, and 3. This endpoint will return a presigned URL for each part
     * number provided, which you can then use to send PUT requests for the binary chunk
     * corresponding to that part. When the part is uploaded, the provider should return an ETag for
     * the part, and this should be stored along with the part number, because this is needed to
     * complete the multipart upload. You must have the correct permissions and CORS settings
     * configured in your external provider. We support AWS S3 as the default. See:
     * https://meta.discourse.org/t/-/210469#s3-multipart-direct-uploads-4. An external file store
     * must be set up and `enable_direct_s3_uploads` must be set to true for this endpoint to
     * function.
     * @param  body  Optional parameter:
     * @return    Returns the UploadsBatchPresignMultipartPartsJsonResponse wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<UploadsBatchPresignMultipartPartsJsonResponse>> batchPresignMultipartPartsAsync(
            final UploadsBatchPresignMultipartPartsJsonRequest body) {
        try {
            return prepareBatchPresignMultipartPartsRequest(body).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for batchPresignMultipartParts.
     */
    private ApiCall<ApiResponse<UploadsBatchPresignMultipartPartsJsonResponse>, ApiException> prepareBatchPresignMultipartPartsRequest(
            final UploadsBatchPresignMultipartPartsJsonRequest body) {
        return new ApiCall.Builder<ApiResponse<UploadsBatchPresignMultipartPartsJsonResponse>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/uploads/batch-presign-multipart-parts.json")
                        .bodyParam(param -> param.value(body).isRequired(false))
                        .bodySerializer(() ->  ApiHelper.serialize(body))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/json").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, UploadsBatchPresignMultipartPartsJsonResponse.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * This endpoint aborts the multipart upload initiated with /create-multipart. This should be
     * used when cancelling the upload. It does not matter if parts were already uploaded into the
     * external storage provider. You must have the correct permissions and CORS settings configured
     * in your external provider. We support AWS S3 as the default. See:
     * https://meta.discourse.org/t/-/210469#s3-multipart-direct-uploads-4. An external file store
     * must be set up and `enable_direct_s3_uploads` must be set to true for this endpoint to
     * function.
     * @param  body  Optional parameter:
     * @return    Returns the UploadsAbortMultipartJsonResponse wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<UploadsAbortMultipartJsonResponse> abortMultipart(
            final UploadsAbortMultipartJsonRequest body) throws ApiException, IOException {
        return prepareAbortMultipartRequest(body).execute();
    }

    /**
     * This endpoint aborts the multipart upload initiated with /create-multipart. This should be
     * used when cancelling the upload. It does not matter if parts were already uploaded into the
     * external storage provider. You must have the correct permissions and CORS settings configured
     * in your external provider. We support AWS S3 as the default. See:
     * https://meta.discourse.org/t/-/210469#s3-multipart-direct-uploads-4. An external file store
     * must be set up and `enable_direct_s3_uploads` must be set to true for this endpoint to
     * function.
     * @param  body  Optional parameter:
     * @return    Returns the UploadsAbortMultipartJsonResponse wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<UploadsAbortMultipartJsonResponse>> abortMultipartAsync(
            final UploadsAbortMultipartJsonRequest body) {
        try {
            return prepareAbortMultipartRequest(body).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for abortMultipart.
     */
    private ApiCall<ApiResponse<UploadsAbortMultipartJsonResponse>, ApiException> prepareAbortMultipartRequest(
            final UploadsAbortMultipartJsonRequest body) {
        return new ApiCall.Builder<ApiResponse<UploadsAbortMultipartJsonResponse>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/uploads/abort-multipart.json")
                        .bodyParam(param -> param.value(body).isRequired(false))
                        .bodySerializer(() ->  ApiHelper.serialize(body))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/json").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, UploadsAbortMultipartJsonResponse.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Completes the multipart upload in the external store, and copies the file from its temporary
     * location to its final location in the store. All of the parts must have been uploaded to the
     * external storage provider. An Upload record will be completed in most cases once the file is
     * copied to its final location. You must have the correct permissions and CORS settings
     * configured in your external provider. We support AWS S3 as the default. See:
     * https://meta.discourse.org/t/-/210469#s3-multipart-direct-uploads-4. An external file store
     * must be set up and `enable_direct_s3_uploads` must be set to true for this endpoint to
     * function.
     * @param  body  Optional parameter:
     * @return    Returns the UploadsCompleteMultipartJsonResponse wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<UploadsCompleteMultipartJsonResponse> completeMultipart(
            final UploadsCompleteMultipartJsonRequest body) throws ApiException, IOException {
        return prepareCompleteMultipartRequest(body).execute();
    }

    /**
     * Completes the multipart upload in the external store, and copies the file from its temporary
     * location to its final location in the store. All of the parts must have been uploaded to the
     * external storage provider. An Upload record will be completed in most cases once the file is
     * copied to its final location. You must have the correct permissions and CORS settings
     * configured in your external provider. We support AWS S3 as the default. See:
     * https://meta.discourse.org/t/-/210469#s3-multipart-direct-uploads-4. An external file store
     * must be set up and `enable_direct_s3_uploads` must be set to true for this endpoint to
     * function.
     * @param  body  Optional parameter:
     * @return    Returns the UploadsCompleteMultipartJsonResponse wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<UploadsCompleteMultipartJsonResponse>> completeMultipartAsync(
            final UploadsCompleteMultipartJsonRequest body) {
        try {
            return prepareCompleteMultipartRequest(body).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for completeMultipart.
     */
    private ApiCall<ApiResponse<UploadsCompleteMultipartJsonResponse>, ApiException> prepareCompleteMultipartRequest(
            final UploadsCompleteMultipartJsonRequest body) {
        return new ApiCall.Builder<ApiResponse<UploadsCompleteMultipartJsonResponse>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/uploads/complete-multipart.json")
                        .bodyParam(param -> param.value(body).isRequired(false))
                        .bodySerializer(() ->  ApiHelper.serialize(body))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/json").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, UploadsCompleteMultipartJsonResponse.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }
}